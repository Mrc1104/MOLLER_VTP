#########################################################################################
# trigger.conf                                                                          #
# Description: Configuration file for which detector(s) and segment(s) that we are		#
#			   interested in obtaining trigger information for. Additionally, the 		#
#			   scalars for the triggers are set here as well.							#
#			  																			#
#			   Format:																	#
#			   # Ring																	#
#			   [a,b,c,...] (Listen to rings a,b,c,...      		                  )     #
#						   ([] or [0] to ignore all rings						  )     #
#						   ([1,2,3,...,8] or [9] to listen to all rings           )     #
#				# Segment																#
#				[a,b,c,...] (Listen to segments a,b,c,...                         )     #
#							([] pr [0] to ignore all segments                     )     #
#							([1,2,3,....,27,28] or [29] to listen to all segments )     #
#				# Scalars																#
#				[a,b,c,...] (Scalars for the corresponding ring triggers		  )		#
#							(Should align index-wise with the # Ring list		  )		#
#							(Defaults to -1 if not specified [never triggers]     )		#
#																					 	#
# Note:		Any lines starting with '#' will be ignored.  								#
#########################################################################################

# Ring
[1,2,3,4,5,6,7,8]

# Segment
[29]

# Scalars
[5,5,5,5,5,5,5,5]
